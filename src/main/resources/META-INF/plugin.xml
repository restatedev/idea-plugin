<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>dev.restate.idea-plugin</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>Restate</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://restate.dev">Restate</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
    IntelliJ IDEA plugin for Restate, a runtime for building distributed applications.<br>
    <ul>
      <li>Create new Restate projects with Java or Kotlin</li>
      <li>Start and stop Restate containers</li>
      <li>Open the Restate UI directly from the IDE</li>
    </ul>
  ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <depends>com.intellij.gradle</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <!-- Register the Restate tool window -->
        <toolWindow id="Restate"
                    secondary="true"
                    icon="AllIcons.Nodes.Plugin"
                    anchor="right"
                    factoryClass="com.example.restate.toolwindow.RestateToolWindowFactory"/>

        <!-- Register the Restate project template -->
        <directoryProjectGenerator implementation="com.example.restate.project.RestateProjectGenerator"/>

        <moduleBuilder builderClass="com.example.restate.project.RestateModuleProjectGenerator" />

        <postStartupActivity implementation="com.example.restate.RestatePluginStartupActivity"/>

        <notificationGroup id="Restate Plugin Notifications"
                           displayType="BALLOON"
                           key="notification.group.name"/>
    </extensions>

    <actions>
        <action id="com.example.restate.actions.StartRestateServerAction"
                class="com.example.restate.actions.StartRestateServerAction"
                text="Start Restate Server"
                description="Start the Restate server">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>
</idea-plugin>
